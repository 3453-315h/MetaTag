[tool:pytest]
testpaths = metatag/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --tb=short
    --strict-markers
    --disable-warnings
markers =
    qt: marks tests requiring Qt event loop
    integration: marks integration tests
    slow: marks slow tests
    unit: marks unit tests